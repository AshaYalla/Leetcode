 select project_id , round(  sum(isnull(experience_years, 0 ))/ cast(count(experience_years) as decimal ) , 2)  as average_years from project p join Employee e on e.employee_id=p.employee_id 
group by project_id